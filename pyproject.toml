[project]
name = "adaptmath"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "langgraph>=1.2.11",
    "langsmith>=0.12.4",
    "openai>=3.13.0",
    "pydantic",
    "uvicorn[standard]>=0.52.4",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
