[project]
name = "adaptive-math-tutor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.2.11",
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
